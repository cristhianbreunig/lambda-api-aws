{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap:test": "lerna bootstrap",
    "bootstrap:prod": "lerna bootstrap -- --production --no-optional",
    "test": "lerna run test"
  }
}